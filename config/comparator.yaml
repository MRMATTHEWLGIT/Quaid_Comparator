# The initial policy to use for the comparator
initial_policy: "uneven"

# The policies to use for each episode
# The length of the playbook determines the number of episodes
episode_playbook:
  - uneven
  - uneven
  - comparator
  - comparator

# The policies available to the comparator
policies:
  flat:
    model_path: "models/onnx/aug_act_net_QuaidSIM-Flat_RA-TD3_+305.334_475000.onnx"

  ramp:
    model_path: "models/onnx/aug_act_net_QuaidSIM-Ramp_RA-TD3_+301.146_475000.onnx"

  uneven:
    model_path: "models/onnx/aug_act_net_QuaidSIM-Uneven_RA-TD3_+348.233_400099.onnx"

# Runtime comparator assets generated by build_comparator.py
comparator_assets_dir: "models/comparator/real_fixed_comparator_parametric_2026-05-17T17-11-02"

# Comparator hyperparameters
hyperparameters:

  # Run expensive comparator policy-selection every N robot-control steps.
  # Example: with a 20 Hz control loop, 5 means comparator runs at 4 Hz.
  comparator_interval_steps: 5

  # GRU input sequence length
  sequence_length: 99

  # Local nearest-neighbour search hyperparameters
  k_signature: 80
  k_reward: 40

  # Candidate filtering hyperparameters
  min_reward_gain_percent: 5.0
  min_distribution_overlap: 0.75
  min_pairwise_agreement: 1.00
  include_current_policy_candidates: true

  # Candidate voting hyperparameters
  min_vote_candidates: 10
  min_vote_fraction: 0.50
  min_vote_margin: 0.10
  required_consecutive_policy_votes: 2
